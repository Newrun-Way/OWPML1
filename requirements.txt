# RAG 시스템 의존성
# Python 3.10+ 기준

# 임베딩 및 벡터 검색
sentence-transformers==3.3.1
chromadb==0.5.5
onnxruntime>=1.14.1  # ChromaDB 의존성
# FAISS (레거시, 필요 시 사용)
# faiss-cpu==1.9.0.post1
# faiss-gpu==1.9.0.post1  # GPU 사용 시 주석 해제

# LLM API
openai>=2.0.0,<3.0.0

# 텍스트 처리 및 청킹 (LangChain)
langchain>=0.1.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0
langchain-core>=0.2.0,<0.3.0
tiktoken==0.8.0

# 데이터 처리
numpy>=1.25.0,<2.0  # LangChain 호환성을 위해 <2.0
transformers==4.57.1

# PyTorch (CPU/GPU)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
# GPU 사용 시: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 파일 처리 (HWP/HWPX)
JPype1==1.6.0

# 유틸리티
python-dotenv==1.0.1
loguru==0.7.3

# RAG 평가
ragas>=0.1.0
datasets>=2.0.0

# 추가 의존성 (자동 설치됨)
# - huggingface-hub
# - safetensors
# - tokenizers
# - pydantic
# - requests
# - tqdm
